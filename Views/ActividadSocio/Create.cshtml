@model ImprovedWorkCenter.Models.ActividadSocio

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ActividadSocio</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ActividadId" class="control-label"></label>
                <select asp-for="ActividadId" class ="form-control" asp-items="ViewBag.ActividadId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SocioId" class="control-label"></label>
                <select asp-for="SocioId" class ="form-control" asp-items="ViewBag.SocioId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
